
<div class="container mt-4">
  <div class="row">
    <div class="col-6 m-auto">
      <div class="flat-form">
        <h1>Modifer l'appartement</h1>
        <div class="mt-3">
          <%= render "form", flat: @flat %>
        </div>
      </div>
    </div>
  </div>
</div>

<%= link_to "Delete", flat_path(@flat), data: {turbo_method: :delete, turbo_confirm: "confirmer la suppression ?"} %>
